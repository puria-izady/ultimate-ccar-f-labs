# Your credential. Copy .env.example to .env and fill that in instead.
.env

# The Python environment, rebuilt by `uv sync`.
.venv/

# Lab working directories. Every one of these is written by a notebook cell,
# so deleting one and rerunning the lab is always safe.
workspace/
out/

__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/
.DS_Store

# Instructor-only material: recording prep and the repo's own test suite.
prep/
tests/
