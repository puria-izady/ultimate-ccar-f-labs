"""The support agent's tools and the policy around them.

`tools` is what the agent can do. `hooks` is what the harness lets it do, and what it
lets it read. Part A proves every piece of both offline; part B hands them to an agent.
"""
