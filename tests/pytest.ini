; labkit's own test settings live here rather than in the repo's pyproject.toml.
; Lab 6 runs `python -m pytest` inside workspace/mycorp, and pytest walks upwards
; looking for an inifile: a [tool.pytest.ini_options] section at the repo root would
; silently apply these settings to the student's run and change its output.
[pytest]
testpaths = .

